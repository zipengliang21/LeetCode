/**
 * @param {string} haystack
 * @param {string} needle
 * @return {number}
 */
const strStr = (haystack, needle) => {
    if (needle === "") return 0;
    for (let i = 0; i < haystack.length; i++) {
        // for (let j = i;) {

        // }
    }
};